{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block main %}
     {% for  category in categories %}
         <a href="{{ path('show_category', { id: category.id}) }}">{{category.name}}</a> <br>
    {% endfor %}
{% endblock %}
