{% extends 'base.html.twig' %}

{% block title %}Categories{% endblock %}

{% block main %}
     {% for  category in categories %}
         <a href="{{ path('sub_category') }}">{{category.name}}</a> <br>
    {% endfor %}
{% endblock %}
